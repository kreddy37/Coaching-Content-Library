/**
 * useContentItem Hook
 *
 * Placeholder for Epic 3 implementation
 * Will fetch single drill from GET /api/v1/content/:id
 *
 * Usage:
 * const { data, isLoading, error } = useContentItem(id);
 */

export function useContentItem(_id?: number): {
  data: null;
  isLoading: boolean;
  error: null;
} {
  // Placeholder implementation for future Epic 3
  return {
    data: null,
    isLoading: false,
    error: null,
  };
}
